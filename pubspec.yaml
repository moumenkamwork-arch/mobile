name: promoo_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.2
  flutter_svg: ^2.3.0
  go_router: ^17.3.0
  dio: ^5.9.2
  supabase_flutter: ^2.16.0
  # Talks to the Realtime websocket directly (no GoTrue/Postgrest overhead —
  # auth is our own backend's). Already pulled in transitively by
  # supabase_flutter; declared explicitly since we import it directly.
  realtime_client: any
  flutter_secure_storage: any
  fpdart: any
  freezed_annotation: any
  json_annotation: any
  intl: ^0.20.2
  flutter_dotenv: ^6.0.1
  flutter_localizations:
    sdk: flutter
  # Push notifications (FCM) — added 2026-07-20.
  firebase_core: ^4.7.0
  firebase_messaging: ^16.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner: any
  freezed: any
  json_serializable: any
  flutter_launcher_icons: ^0.14.2

flutter_icons:
  android: true
  ios: true
  image_path: "assets/brand/new_logo/launcher_icon.png"
  adaptive_icon_background: "#000000"
  adaptive_icon_foreground: "assets/brand/new_logo/launcher_icon.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # Generates AppLocalizations from the ARB files in lib/l10n (see l10n.yaml).
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - .env
    - assets/brand/
    - assets/brand/icons/
    - assets/brand/social/
    - assets/brand/new_logo/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # Tajawal is the recommended Promoo UI font (see docs/design/brand_identity.md).
  # Bundled directly as .ttf assets (sourced from the Google Fonts OFL repo;
  # license at assets/fonts/tajawal/OFL.txt) instead of the google_fonts package,
  # which failed to resolve on this Windows setup without Developer Mode symlinks.
  fonts:
    - family: Tajawal
      fonts:
        - asset: assets/fonts/tajawal/Tajawal-ExtraLight.ttf
          weight: 200
        - asset: assets/fonts/tajawal/Tajawal-Light.ttf
          weight: 300
        - asset: assets/fonts/tajawal/Tajawal-Regular.ttf
          weight: 400
        - asset: assets/fonts/tajawal/Tajawal-Medium.ttf
          weight: 500
        - asset: assets/fonts/tajawal/Tajawal-Bold.ttf
          weight: 700
        - asset: assets/fonts/tajawal/Tajawal-ExtraBold.ttf
          weight: 800
        - asset: assets/fonts/tajawal/Tajawal-Black.ttf
          weight: 900

    # Varela Round is logo-only (see AGENTS.md: "Logo font: Varela Round, logo
    # only"). Used for the animated splash wordmark and the recreated bold
    # "PROMOO" header logo. License at assets/fonts/varela_round/OFL.txt.
    - family: VarelaRound
      fonts:
        - asset: assets/fonts/varela_round/VarelaRound-Regular.ttf
          weight: 400
